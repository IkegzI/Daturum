<%= form_for :correction, url: correction_create_path, class: :form_correction do |f| %>
    <p>
        <p onclick=""><%= f.label 'Корректировка ответа:',  %></p>
        <%= f.text_field :text %>

        <%= f.hidden_field :answer_id, value: @q.id %>
    </p>
    <%= f.submit 'Сохранить' %>
<% end %>
